"use strict";

var sdkPackage = require('../../package.json');

var sdkDetails = statusCode => {
  console.log("".concat(sdkPackage.name, " Version: ").concat(sdkPackage.version, " status code:"), statusCode);
};

process.on('beforeExit', sdkDetails);